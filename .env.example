# Docker Compose - Country Guesser
# Copier en .env et adapter si besoin

# Images Docker (pour publier sur un registry)
# Exemples:
# - IMAGE_PREFIX=ghcr.io/<user_or_org>/countryguesser
# - IMAGE_TAG=1.0.0
IMAGE_PREFIX=countryguesser
IMAGE_TAG=latest

# MySQL
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=CountryGuesserDB
MYSQL_USER=countryguesser
MYSQL_PASSWORD=countryguesser

# Optionnel : token Mapbox pour la carte (client)
# REACT_APP_MAPBOX_TOKEN=pk.xxx

# Optionnel : override des endpoints côté client (par défaut /api et /ws via Nginx)
# REACT_APP_API_URI=/api
# REACT_APP_WEBSOCKET_URI=/ws
